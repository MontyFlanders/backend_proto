{
  "name": "antiquity-aws",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "synth": "npm run build && cdk synth",
    "deploy": "npm run build && cdk deploy",
    "diff": "npm run build && cdk diff",
    "destroy": "cdk destroy"
  },
  "devDependencies": {
    "@aws-cdk/aws-neptune-alpha": "^2.219.0-alpha.0",
    "@types/node": "^20.11.30",
    "aws-cdk-lib": "^2.150.0",
    "constructs": "^10.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
